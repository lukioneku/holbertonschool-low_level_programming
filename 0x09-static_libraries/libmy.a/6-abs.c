#include "main.h"

/**
  * _abs - detects absolute value of the integer
  * @e: interget that was converted
  * Return: absolute value
 */
int _abs(int e)
{
	if (e >= 0)
	return (e);

	else
		e = e * -1;
	return (e);
}
